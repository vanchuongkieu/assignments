import styled from "styled-components";

export const HomeTop = styled.div`
  display: flex;
  gap: 50px;
  margin: 15px 0 0;

  & img {
    width: 100%;
    object-fit: cover;
    height: 100%;
    user-select: none;
  }
`;
