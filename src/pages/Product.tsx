import { StyledContainer } from "@/layouts/client/StyledLayout";

type Props = {};

const Product = (props: Props) => {
  return <StyledContainer>Product</StyledContainer>;
};

export default Product;
